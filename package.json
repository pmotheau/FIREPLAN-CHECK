{
  "name": "fireplan-checker",
  "version": "1.2.0",
  "description": "Outil pour vérifier facilement des plans de sécurité incendie",
  "main": "src/main.js",
  "scripts": {
    "start": "electron .",
    "package": "electron-packager . FirePlanChecker --platform=win32 --arch=x64 --out=release-builds --icon=src/assets/icons/favicon.ico"
  },
  "build": {
    "icon": "build/favicon.ico"
  },
  "dependencies": {
    "express": "^4.17.1",
    "express-fileupload": "^1.2.1"
  },
  "devDependencies": {
    "electron": "^13.1.7",
    "electron-packager": "^15.0.0"
  }
}